# 📋 سياق مشروع صَوْن (Sawn) - النسخة النهائية الكاملة

> **تعليمات:** هذا الملف يحتوي على كل سياق المشروع. عند بدء محادثة جديدة مع Claude في Cursor، اطلب منه قراءة هذا الملف أولاً.

---

## 🎯 نظرة عامة على المشروع

| العنصر | التفاصيل |
|--------|----------|
| **اسم التطبيق** | صَوْن (Sawn) |
| **الشعار الدعائي** | "كل مستنداتك في جيبك" |
| **الفكرة** | تطبيق لحفظ وإدارة المستندات الشخصية والمهنية بطريقة ذكية |
| **السوق المستهدف** | السوق العربي/الخليجي أولاً، ثم التوسع عالمياً |
| **المطور** | صالح |
| **الإصدار الأول** | مجاني بالكامل (الاشتراكات في إصدارات لاحقة) |

---

## 🏗️ البنية التقنية

### النموذج: Google Drive + Supabase + Local Storage

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   Google Drive              Supabase              Local         │
│   ────────────              ────────              ─────         │
│   • ملفات المستندات         • بيانات المستخدم      • Cache       │
│   • الصور و PDFs            • metadata             • Offline     │
│   • النسخ الاحتياطي         • التذكيرات            • PIN         │
│                                                                 │
│                         التطبيق (صَوْن)                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### لماذا هذا النموذج؟
- ✅ تخزين غير محدود مجاناً (على حساب المستخدم)
- ✅ ملفات المستخدم في حسابه الشخصي
- ✅ استعادة سهلة عند تغيير الجهاز
- ✅ يعمل بدون إنترنت (Offline mode)

---

## 🔐 التسجيل والمصادقة

### الطريقة: Google Sign-In فقط

```
تسجيل الدخول
     ↓
إنشاء/ربط مجلد "صَوْن" في Drive
     ↓
إعداد PIN (اختياري)
     ↓
جاهز للاستخدام
```

### إعداد PIN:
- **اختياري** - المستخدم يختار تفعيله أو لا
- **محاولات غير محدودة** - بدون قفل
- **نسيت الرمز** → زر يفتح اختيار Google → مطابقة الحساب → إنشاء رمز جديد

### استعادة البيانات (جهاز جديد):
```
تسجيل بنفس حساب Google
          ↓
    "وجدنا بياناتك!"
          ↓
  [استعادة] أو [بدء جديد]
          ↓
     إعداد PIN جديد
```

### حذف المجلد يدوياً:
```
إذا المستخدم حذف مجلد صَوْن من Drive:
     ↓
"لم نجد المجلد السابق. قد يكون حُذف من حسابك في Google Drive."
     ↓
"سيتم إنشاء مجلد جديد"
     ↓
    [موافق]
```

---

## 🎨 الهوية البصرية - التصميم النظيف (Clean Design)

### فلسفة التصميم:
```
مستوحى من تطبيقات Google الحديثة
• مساحات بيضاء كثيرة
• ألوان محدودة (2-3 فقط)
• أيقونات Outlined
• بدون ظلال أو ظلال خفيفة جداً
• تركيز على المحتوى
```

### الشعار:
| العنصر | القرار |
|--------|--------|
| **الشكل** | حرف ص يلتف ويحتضن |
| **الألوان** | تدرج أزرق → بنفسجي (للشعار فقط) |
| **كود الألوان** | `#1A73E8` → `#8B5CF6` |

### الخطوط:
| اللغة | الخط |
|-------|------|
| **العربية** | Rubik |
| **الإنجليزية** | Poppins |

### الألوان:
```dart
// اللون الأساسي (يُستخدم باعتدال)
primary: #1A73E8         // أزرق Google
onPrimary: #FFFFFF

// الخلفيات
background: #FFFFFF       // أبيض نقي
surface: #FAFAFA          // رمادي فاتح جداً
surfaceVariant: #F1F3F4

// النصوص
textPrimary: #202124      // أسود Google
textSecondary: #5F6368
textTertiary: #9AA0A6

// الحدود
border: #DADCE0
borderLight: #E8EAED

// الحالات
error: #D93025
success: #1E8E3E
warning: #F9AB00

// الأيقونات
iconPrimary: #5F6368
iconActive: #1A73E8
```

---

## 📱 تجربة المستخدم (UX) - القرارات النهائية

### Onboarding:
- **3 شاشات** + شرح بسيط لماذا نستخدم Google Drive
- شاشة 1: "احفظ مستنداتك بأمان"
- شاشة 2: "تذكيرات قبل الانتهاء"
- شاشة 3: "ملفاتك في حسابك" (شرح Drive)

### الشاشة الرئيسية:
```
┌─────────────────────────────────────────┐
│                                         │
│   مرحباً، [الاسم]                       │
│   [عدد] مستندات تنتهي قريباً            │
│                                         │
│   ┌─────────────────────────────────┐   │
│   │ ○  البحث في المستندات           │   │
│   └─────────────────────────────────┘   │
│                                         │
│   ⭐ المفضلة (اختصارات سريعة)          │
│   [بطاقة] [بطاقة] [بطاقة]              │
│                                         │
│   ⚠️ تنتهي قريباً                       │
│   [قائمة المستندات]                    │
│                                         │
│   📁 التصنيفات                          │
│   [شخصي] [سيارة] [عمل] [سكن]           │
│                                         │
├─────────────────────────────────────────┤
│   ○ الرئيسية  ○ ملفاتي  (+)  ○ الإعدادات │
└─────────────────────────────────────────┘
```

### المميزات الرئيسية:
| الميزة | التفاصيل |
|--------|----------|
| **المفضلة** | اختصارات للمستندات المهمة (تظهر في الرئيسية) |
| **الفلترة** | فلترة حسب التصنيف، النوع، الحالة |
| **البحث** | بحث سريع في العناوين والأرقام |
| **Offline** | تحميل المستندات للاستخدام بدون إنترنت |

---

## 📄 إضافة مستند

### الخطوات:
```
1. اختيار طريقة الإضافة:
   • التقاط صورة 📷
   • رفع PDF 📄
   • من المعرض 🖼️

2. تصوير/اختيار الملف

3. سؤال: "هل تريد إضافة صورة أخرى؟" (للوجه الخلفي)
   • ليس تلقائي - المستخدم يختار

4. تحليل OCR (إن أمكن)
   • إذا نجح: عرض البيانات المستخرجة للمراجعة
   • إذا فشل: إدخال يدوي

5. إكمال البيانات:
   • العنوان
   • التصنيف (شخصي/سيارة/عمل/سكن)
   • رقم المستند (اختياري)
   • تاريخ الإصدار (اختياري)
   • تاريخ الانتهاء (اختياري)
   • الملاحظات (اختياري) ← حقل نص حر

6. إعداد التذكيرات:
   • "كم تريد من التذكيرات؟"
   • اختيار متعدد: قبل 7 أيام، 30 يوم، 60 يوم، مخصص
   
7. حفظ
   • رفع لـ Google Drive
   • حفظ metadata في Supabase
   • حفظ محلي للـ Offline
```

### بيانات المستند:
```dart
Document {
  id: String
  title: String                    // إجباري
  category: String                 // إجباري
  documentType: String?            // نوع المستند (هوية، جواز، إلخ)
  documentNumber: String?          // رقم المستند
  issueDate: Date?                 // تاريخ الإصدار
  expiryDate: Date?                // تاريخ الانتهاء
  notes: String?                   // ملاحظات ← جديد
  isFavorite: bool                 // مفضلة ← جديد
  isDownloadedOffline: bool        // محمل للـ Offline ← جديد
  driveFileId: String
  driveFileUrl: String
  extractedData: JSON?             // بيانات OCR
  createdAt: DateTime
  updatedAt: DateTime
}
```

---

## 🔔 التذكيرات

### إعداد التذكيرات:
- المستخدم يختار عدد التذكيرات عند إضافة المستند
- خيارات جاهزة: 7 أيام، 30 يوم، 60 يوم، 90 يوم
- خيار مخصص: يحدد التاريخ بنفسه
- يمكن إضافة أكثر من تذكير لنفس المستند

### عند استلام التذكير:
```
🔔 إشعار: "بطاقة الهوية تنتهي بعد 30 يوم"
                    ↓
         يضغط على الإشعار
                    ↓
         يفتح تفاصيل المستند
                    ↓
         خيارات:
         • تأجيل التذكير
         • تم التجديد (تحديث المستند)
         • تجاهل
```

### عند التجديد:
- نسأل: "هل تريد تحديث صورة المستند؟"
- **لا نحتفظ بالنسخ القديمة** - يتم الاستبدال

---

## 🔍 البحث والفلترة

### البحث:
- بحث سريع في: العناوين، أرقام المستندات، الملاحظات
- نتائج فورية أثناء الكتابة

### الفلترة:
```
┌─────────────────────────────────────────┐
│ فلترة حسب:                              │
│                                         │
│ التصنيف:                                │
│ [الكل] [شخصي] [سيارة] [عمل] [سكن]       │
│                                         │
│ الحالة:                                 │
│ [الكل] [ساري] [ينتهي قريباً] [منتهي]    │
│                                         │
│ النوع:                                  │
│ [الكل] [هوية] [جواز] [رخصة] [عقد] ...   │
│                                         │
│           [تطبيق] [إعادة ضبط]           │
└─────────────────────────────────────────┘
```

---

## ⭐ المفضلة

### الوظيفة:
- المستخدم يضيف مستندات للمفضلة
- تظهر في الشاشة الرئيسية كاختصارات سريعة
- وصول سريع بضغطة واحدة

### طريقة الإضافة:
- من تفاصيل المستند: أيقونة ⭐
- أو من القائمة: سحب أو قائمة الخيارات

---

## 📶 الوضع بدون إنترنت (Offline)

### التخزين المحلي:
- **غير محدود** - نخزن كل المستندات محلياً
- **أي حجم** - نحمل الصور بحجمها الكامل

### خيار "تحميل للاستخدام Offline":
```
في تفاصيل المستند:
┌─────────────────────────────────────────┐
│ ☁️ تحميل للاستخدام بدون إنترنت         │
│    [تحميل] أو [تم التحميل ✓]            │
└─────────────────────────────────────────┘
```

### عند فقدان الاتصال:
```
الشاشة الرئيسية:
🔴 "أنت غير متصل بالإنترنت"

- عرض المستندات المحملة ✅
- إضافة مستند جديد → "سيُحفظ ويُرفع عند الاتصال"
- المزامنة تلقائية عند عودة الإنترنت
```

---

## 🔒 الأمان

### قفل PIN:
- **اختياري** - المستخدم يختار تفعيله
- 4 أرقام
- **محاولات غير محدودة**
- خيار البصمة/Face ID متاح

### نسيت الرمز:
```
شاشة إدخال PIN
       ↓
"نسيت الرمز؟" (رابط)
       ↓
اختيار حساب Google
       ↓
مطابقة الحساب مع المسجل
       ↓
"تم التحقق! أنشئ رمز جديد"
       ↓
إدخال رمز جديد
```

### طبقات الحماية:
```
1. Google Auth      → تسجيل آمن
2. PIN/بصمة (اختياري) → قفل التطبيق
3. Google Drive     → تشفير Google
4. Supabase RLS     → عزل البيانات
```

---

## 🔗 المشاركة

### طريقة المشاركة:
```
تفاصيل المستند → زر "مشاركة"
                    ↓
            خيارات المشاركة:
            • مشاركة صورة
            • مشاركة PDF
                    ↓
            تحذير أمني:
            "أنت على وشك مشاركة مستند
             يحتوي بيانات شخصية"
                    ↓
            [متابعة] [إلغاء]
                    ↓
            اختيار تطبيق المشاركة
```

### ملاحظات المشاركة:
- ❌ **لا** نضيف علامة مائية
- ❌ **لا** نسجل تاريخ المشاركة
- ❌ **لا** نضيف إخفاء للبيانات (blur)

---

## 🗑️ حذف الحساب

### الخطوات:
```
الإعدادات → الحساب → حذف الحساب
                 ↓
         تحذير بما سيُحذف
                 ↓
         سبب الحذف (اختياري):
         ┌─────────────────────────────┐
         │ اختر السبب: [قائمة منسدلة] │
         │ • وجدت تطبيق أفضل          │
         │ • لا أحتاج التطبيق         │
         │ • مشاكل تقنية              │
         │ • خصوصية                   │
         │ • أخرى                     │
         │                            │
         │ ملاحظات إضافية (اختياري):  │
         │ ┌────────────────────────┐ │
         │ │                        │ │
         │ └────────────────────────┘ │
         └─────────────────────────────┘
                 ↓
         كتابة "حذف" للتأكيد
                 ↓
         تأكيد هوية Google
                 ↓
              تم الحذف
```

### ما يُحذف:
- ✅ بيانات المستخدم من Supabase
- ✅ البيانات المحلية
- ❌ مجلد صَوْن في Drive (يبقى - المستخدم يحذفه يدوياً)

---

## 🗄️ قاعدة البيانات (Supabase)

### جدول المستخدمين (users):
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY,
  google_id TEXT UNIQUE NOT NULL,
  email TEXT NOT NULL,
  name TEXT,
  avatar_url TEXT,
  drive_folder_id TEXT,
  pin_enabled BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### جدول التصنيفات (categories):
```sql
CREATE TABLE categories (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  name_ar TEXT NOT NULL,
  name_en TEXT NOT NULL,
  icon TEXT,
  drive_folder_id TEXT,
  is_default BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### جدول المستندات (documents):
```sql
CREATE TABLE documents (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  category_id UUID REFERENCES categories(id),
  title TEXT NOT NULL,
  document_type TEXT,
  document_number TEXT,
  issue_date DATE,
  expiry_date DATE,
  notes TEXT,                          -- ملاحظات
  is_favorite BOOLEAN DEFAULT false,   -- مفضلة
  is_offline BOOLEAN DEFAULT false,    -- محمل Offline
  drive_file_id TEXT,
  drive_file_url TEXT,
  extracted_data JSONB,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### جدول التذكيرات (reminders):
```sql
CREATE TABLE reminders (
  id UUID PRIMARY KEY,
  document_id UUID REFERENCES documents(id),
  user_id UUID REFERENCES users(id),
  remind_date DATE NOT NULL,
  days_before INTEGER NOT NULL,
  is_sent BOOLEAN DEFAULT false,
  is_read BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### جدول أسباب الحذف (deletion_reasons):
```sql
CREATE TABLE deletion_reasons (
  id UUID PRIMARY KEY,
  reason TEXT NOT NULL,
  notes TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 📂 هيكل Google Drive

```
Google Drive/
└── 📁 صَوْن (Sawn)/
    ├── 📁 شخصي/
    │   ├── 📄 بطاقة_الهوية.jpg
    │   ├── 📄 جواز_السفر.pdf
    │   └── ...
    ├── 📁 السيارة/
    ├── 📁 العمل/
    └── 📁 السكن/
```

---

## 💰 نموذج الربح

### الإصدار الأول (الحالي):
```
🆓 مجاني بالكامل
• مستندات غير محدودة
• تخزين غير محدود (Google Drive)
• جميع المميزات متاحة
• بدون إعلانات
```

### الإصدارات اللاحقة (مستقبلاً):
- باقات مدفوعة لمميزات متقدمة
- OCR متقدم
- مشاركة عائلية
- تقارير وإحصائيات

---

## 🛠️ التقنيات

| التقنية | الاستخدام |
|---------|-----------|
| **Flutter 3.x** | التطبيق |
| **google_sign_in** | المصادقة |
| **googleapis** | Google Drive API |
| **Supabase** | قاعدة البيانات |
| **Riverpod** | State Management |
| **GoRouter** | Navigation |
| **Google ML Kit** | OCR |
| **flutter_secure_storage** | تخزين PIN |
| **local_auth** | البصمة |
| **Hive/Isar** | التخزين المحلي |

---

## ✅ ملخص المميزات

### متاح في الإصدار الأول:
- [x] تسجيل بـ Google
- [x] حفظ مستندات غير محدود
- [x] تخزين في Google Drive
- [x] تصنيفات (شخصي، سيارة، عمل، سكن)
- [x] تذكيرات متعددة
- [x] OCR لاستخراج البيانات
- [x] بحث وفلترة
- [x] مفضلة واختصارات
- [x] قفل PIN/بصمة (اختياري)
- [x] وضع Offline
- [x] مشاركة المستندات
- [x] ملاحظات للمستندات

### للإصدارات اللاحقة:
- [ ] الاشتراكات المدفوعة
- [ ] OCR متقدم
- [ ] مشاركة عائلية
- [ ] تقارير وإحصائيات
- [ ] Widget للشاشة الرئيسية
- [ ] Apple Sign-In

---

## 📝 المهام القادمة

### المرحلة 1 - الأساس:
- [ ] تحديث شاشات Auth لـ Google Sign-In
- [ ] إنشاء Google Drive Service
- [ ] إنشاء Supabase Service
- [ ] تحديث الشاشات للتصميم النظيف

### المرحلة 2 - المستندات:
- [ ] شاشة إضافة مستند (كاملة)
- [ ] شاشة تفاصيل المستند
- [ ] شاشة قائمة المستندات
- [ ] البحث والفلترة
- [ ] المفضلة

### المرحلة 3 - OCR:
- [ ] ربط Google ML Kit
- [ ] استخراج البيانات
- [ ] معالجة الفشل (إدخال يدوي)

### المرحلة 4 - التذكيرات:
- [ ] إعداد التذكيرات
- [ ] الإشعارات المحلية
- [ ] شاشة التذكيرات

### المرحلة 5 - Offline:
- [ ] التخزين المحلي
- [ ] المزامنة
- [ ] تحميل للـ Offline

---

## 🚀 نصائح التنفيذ

1. **ابدأ بـ Google Sign-In** - الأساس لكل شيء
2. **اختبر Drive API** - تأكد من إنشاء المجلدات
3. **صمم Offline-first** - التطبيق يعمل بدون نت
4. **اختبر على جهاز حقيقي** - الكاميرا والبصمة
5. **لا تؤجل الأمان** - RLS من البداية

---

> **آخر تحديث:** النسخة النهائية مع كل قرارات تجربة المستخدم
